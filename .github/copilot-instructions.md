# Sodapop instructions

Read [the root AGENTS.md](../AGENTS.md) first; it is the canonical repository map.
Before editing, read the `AGENTS.md` files along each affected file's directory path
and follow relevant code/document links. Scoped guidance supplements the root.
Load only the guides needed for the task, not the entire instruction tree.
Keep repository policy in those guides rather than duplicating it here.
